name: empregos_app
description: "Aplicativo para centralizar oportunidades de emprego e qualificação profissional."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  provider: ^6.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8
  url_launcher: ^6.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
#    - assets/images/
#    - assets/icons/
    - assets/job_mock.json
    - assets/qualification_mock.json
